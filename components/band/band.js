/**
* band JS
* -----------------------------------------------------------------------------
*
* All the JS for the band component.
*/
( function( app ) {

  var COMPONENT = {

    // Fires after common.init, before .finalize and common.finalize
    init: function() {

    },

    finalize: function() {

    }
  };

  // Hooks the component into the app
  app.registerComponent( 'band', COMPONENT );
} )( app );
